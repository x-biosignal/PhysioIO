% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-repositories.R
\name{downloadZenodo}
\alias{downloadZenodo}
\title{Download files from a public Zenodo record}
\usage{
downloadZenodo(
  record_id,
  dest = ".",
  files = NULL,
  verify_md5 = TRUE,
  overwrite = FALSE,
  quiet = TRUE,
  api_url = "https://zenodo.org/api"
)
}
\arguments{
\item{record_id}{Zenodo record ID (numeric or character).}

\item{dest}{Destination directory (created if needed). Default \code{"."}.}

\item{files}{Optional character vector of file names; restricts the download
to those files (default \code{NULL} = every file in the record).}

\item{verify_md5}{Logical; if \code{TRUE} (default) each file is checked against
the Zenodo MD5 and a mismatch is an error.}

\item{overwrite}{Logical; re-download files already present in \code{dest}
(default \code{FALSE}; an existing file with a known-bad checksum is still
re-downloaded).}

\item{quiet}{Logical; suppress the per-file download progress (default
\code{TRUE}).}

\item{api_url}{Zenodo REST API base URL.}
}
\value{
A data frame (one row per file) with columns \code{name}, \code{path}, \code{size},
\code{checksum}, \code{algo} (\code{"md5"}) and \code{checksum_ok} (\code{TRUE}/\code{FALSE}, or \code{NA}
when no checksum was available), invisibly.
}
\description{
Downloads the files of a public Zenodo \emph{record} into \code{dest} via the Zenodo
public REST API, optionally verifying each file against the MD5 checksum
Zenodo reports. No authentication is used, so only open/public records are
reachable. Both the legacy file-array response and the newer InvenioRDM
\code{files.entries} response are handled.
}
\details{
The \code{record_id} is the numeric identifier of a Zenodo record -- the trailing
number of a Zenodo DOI or URL (e.g. \code{3374790} in \code{10.5281/zenodo.3374790}).
Note that a Zenodo \emph{concept} DOI resolves to the latest version; pass a
specific version's record ID to pin a version.
}
\examples{
\dontrun{
info <- downloadZenodo(3374790, dest = tempdir())
info$path
}
}
\seealso{
\code{\link[=downloadFigshare]{downloadFigshare()}}, \code{\link[=downloadMendeley]{downloadMendeley()}}, \code{\link[=downloadFromDOI]{downloadFromDOI()}},
\code{\link[=downloadPhysioNet]{downloadPhysioNet()}}
}
