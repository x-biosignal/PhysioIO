% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-repositories.R
\name{downloadMendeley}
\alias{downloadMendeley}
\title{Download files from a Mendeley Data dataset}
\usage{
downloadMendeley(
  dataset_id,
  dest = ".",
  files = NULL,
  version = NULL,
  access_token = Sys.getenv("MENDELEY_TOKEN", ""),
  verify_hash = TRUE,
  overwrite = FALSE,
  quiet = TRUE,
  folder_id = "root",
  api_url = "https://data.mendeley.com/public-api"
)
}
\arguments{
\item{dataset_id}{Mendeley dataset id (or a full DOI/URL).}

\item{dest}{Destination directory (created if needed). Default \code{"."}.}

\item{files}{Optional character vector of file names; restricts the download
to those files (default \code{NULL} = every file in the folder).}

\item{version}{Optional integer dataset version; \code{NULL} (default) resolves to
the latest published version.}

\item{access_token}{Mendeley OAuth bearer token; defaults to the
\code{MENDELEY_TOKEN} environment variable. Required to download file bytes.}

\item{verify_hash}{Logical; if \code{TRUE} (default) each file is checked against
the Mendeley SHA-256 and a mismatch is an error.}

\item{overwrite}{Logical; re-download files already present in \code{dest}
(default \code{FALSE}).}

\item{quiet}{Logical; suppress the per-file download progress (default
\code{TRUE}).}

\item{folder_id}{Folder to download from (default \code{"root"}).}

\item{api_url}{Mendeley Data public API base URL.}
}
\value{
A data frame (one row per file) with columns \code{name}, \code{path}, \code{size},
\code{checksum}, \code{algo} (\code{"sha256"}) and \code{checksum_ok}, invisibly.
}
\description{
Downloads the files of a Mendeley Data dataset into \code{dest}, verifying each
against the SHA-256 hash Mendeley reports.
}
\details{
Unlike figshare and Zenodo, Mendeley Data exposes dataset and file
\emph{metadata} publicly (see \code{\link[=mendeleyDatasetFiles]{mendeleyDatasetFiles()}}) but requires an \strong{OAuth
access token} to download the file \emph{bytes}. Supply the token via
\code{access_token} or the \code{MENDELEY_TOKEN} environment variable; obtain one
through the Mendeley Data API (an OAuth2 application). Without a token this
function errors early and points you at \code{\link[=mendeleyDatasetFiles]{mendeleyDatasetFiles()}} for the
public manifest.

The \code{dataset_id} is the Mendeley Data identifier -- the alphanumeric part of
a Mendeley DOI \verb{10.17632/<id>.<version>} or a
\verb{data.mendeley.com/datasets/<id>/<version>} URL.
}
\examples{
\dontrun{
# metadata is public:
mendeleyDatasetFiles("mzpz22x58x")
# bytes need a token:
downloadMendeley("mzpz22x58x", dest = tempdir(),
                 access_token = Sys.getenv("MENDELEY_TOKEN"))
}
}
\seealso{
\code{\link[=mendeleyDatasetFiles]{mendeleyDatasetFiles()}}, \code{\link[=downloadFigshare]{downloadFigshare()}}, \code{\link[=downloadZenodo]{downloadZenodo()}},
\code{\link[=downloadFromDOI]{downloadFromDOI()}}
}
