% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-hdf5.R
\name{writeAssayHDF5}
\alias{writeAssayHDF5}
\title{Write assay to HDF5 file}
\usage{
writeAssayHDF5(x, path, assay_name, compression_level = 6L)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{path}{Path to the HDF5 file.}

\item{assay_name}{Character string naming the assay to write.}

\item{compression_level}{Integer compression level (0-9).}
}
\value{
Invisible \code{NULL}. The assay is written to the HDF5 file as a
side effect.
}
\description{
Writes a single assay to an existing HDF5 file.
}
\examples{
# Create a PhysioExperiment and write it to a temporary HDF5 file
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000), nrow = 100, ncol = 10)),
  samplingRate = 100
)
tmp <- tempfile(fileext = ".h5")
writePhysioHDF5(pe, tmp)

# Add a second assay to the object and write only that assay to the file
SummarizedExperiment::assay(pe, "scaled") <-
  SummarizedExperiment::assay(pe, "raw") * 2
writeAssayHDF5(pe, tmp, "scaled")

unlink(tmp)
}
\references{
The HDF Group (1997-2024). "Hierarchical Data Format, version 5."
\url{https://www.hdfgroup.org/HDF5/}
}
\seealso{
\code{\link{writePhysioHDF5}}, \code{\link{readPhysioHDF5}},
\code{\link{realizeHDF5}}
}
