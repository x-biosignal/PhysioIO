% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-repositories.R
\name{figshareCollectionArticles}
\alias{figshareCollectionArticles}
\title{List the articles in a public figshare collection}
\usage{
figshareCollectionArticles(
  collection_id,
  api_url = "https://api.figshare.com/v2"
)
}
\arguments{
\item{collection_id}{figshare collection ID (numeric or character); a full
collection DOI/URL is also tolerated.}

\item{api_url}{figshare REST API base URL.}
}
\value{
A data frame with columns \code{article_id}, \code{title} and \code{doi}, one row
per article (up to the API page-size cap of 1000).
}
\description{
Returns the articles that make up a public figshare \emph{collection} (DOI
\verb{10.6084/m9.figshare.c.<id>}), so each can be fetched with
\code{\link[=downloadFigshare]{downloadFigshare()}}. Collections (e.g. GaitRec, \code{c.4788012}) bundle several
articles; figshare has no single-call "download the whole collection", so the
idiom is list-then-download-per-article.
}
\examples{
\dontrun{
arts <- figshareCollectionArticles(4788012)   # GaitRec
downloadFigshare(arts$article_id[1], dest = tempdir())
}
}
\seealso{
\code{\link[=downloadFigshare]{downloadFigshare()}}
}
