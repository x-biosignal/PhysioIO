% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-arrow.R
\name{registerParquetAssay}
\alias{registerParquetAssay}
\title{Register a Parquet-backed assay as a DuckDB view for out-of-core queries}
\usage{
registerParquetAssay(con, dir, view_name = "signals", assay = NULL)
}
\arguments{
\item{con}{A DuckDB connection (from \code{\link[=connectDatabase]{connectDatabase()}}).}

\item{dir}{A directory written by \code{\link[=writeParquet]{writeParquet()}}.}

\item{view_name}{Name of the DuckDB view to create (default \code{"signals"}).}

\item{assay}{Assay to expose; defaults to the manifest's default assay.}
}
\value{
The \code{view_name}, invisibly.
}
\description{
Creates (or replaces) a DuckDB view over an assay Parquet file written by
\code{\link[=writeParquet]{writeParquet()}}, using DuckDB's \code{read_parquet()} so that aggregates are
computed directly over the file without loading it into R. The view columns
are \code{ch1}, \code{ch2}, ... (one per channel).
}
\seealso{
\code{\link[=writeParquet]{writeParquet()}}, \code{\link[=registerExperiment]{registerExperiment()}}
}
