% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-repositories.R
\name{downloadFigshare}
\alias{downloadFigshare}
\title{Download files from a public figshare article}
\usage{
downloadFigshare(
  article_id,
  dest = ".",
  files = NULL,
  version = NULL,
  verify_md5 = TRUE,
  overwrite = FALSE,
  quiet = TRUE,
  api_url = "https://api.figshare.com/v2"
)
}
\arguments{
\item{article_id}{figshare article ID (numeric or character).}

\item{dest}{Destination directory (created if needed). Default \code{"."}.}

\item{files}{Optional character vector of file names; restricts the download
to those files (default \code{NULL} = every file in the article).}

\item{version}{Optional integer article version; \code{NULL} (default) fetches the
latest public version.}

\item{verify_md5}{Logical; if \code{TRUE} (default) each file is checked against
the figshare MD5 and a mismatch is an error.}

\item{overwrite}{Logical; re-download files already present in \code{dest}
(default \code{FALSE}; an existing file with a known-bad checksum is still
re-downloaded).}

\item{quiet}{Logical; suppress the per-file download progress (default
\code{TRUE}).}

\item{api_url}{figshare REST API base URL.}
}
\value{
A data frame (one row per file) with columns \code{name}, \code{path}, \code{size},
\code{checksum}, \code{algo} (\code{"md5"}) and \code{checksum_ok} (\code{TRUE}/\code{FALSE}, or \code{NA}
when no checksum was available), invisibly.
}
\description{
Downloads the files attached to a public figshare \emph{article} into \code{dest} via
the figshare public REST API, optionally verifying each file against the MD5
checksum figshare reports. No authentication is used, so only public
(published) articles are reachable.
}
\details{
The \code{article_id} is the numeric identifier of a figshare article -- the
trailing number of a figshare DOI or URL (e.g. \code{7636887} in
\code{10.6084/m9.figshare.7636887}). For a figshare \emph{collection} (DOI
\verb{...figshare.c.<id>}), list its member articles first with
\code{\link[=figshareCollectionArticles]{figshareCollectionArticles()}} and call this function on each.
}
\examples{
\dontrun{
# Download every file of a figshare article into a temporary directory
info <- downloadFigshare(7636887, dest = tempdir())
info$path
}
}
\seealso{
\code{\link[=downloadZenodo]{downloadZenodo()}}, \code{\link[=downloadMendeley]{downloadMendeley()}}, \code{\link[=downloadFromDOI]{downloadFromDOI()}},
\code{\link[=figshareCollectionArticles]{figshareCollectionArticles()}}, \code{\link[=downloadPhysioNet]{downloadPhysioNet()}}
}
