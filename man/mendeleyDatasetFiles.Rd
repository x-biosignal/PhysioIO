% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-repositories.R
\name{mendeleyDatasetFiles}
\alias{mendeleyDatasetFiles}
\title{List the files of a public Mendeley Data dataset}
\usage{
mendeleyDatasetFiles(
  dataset_id,
  version = NULL,
  folder_id = "root",
  api_url = "https://data.mendeley.com/public-api"
)
}
\arguments{
\item{dataset_id}{Mendeley dataset id (or a full DOI/URL).}

\item{version}{Optional integer dataset version; \code{NULL} (default) resolves to
the latest published version.}

\item{folder_id}{Folder to list (default \code{"root"}; Mendeley organises files
into folders and only one folder level is listed per call).}

\item{api_url}{Mendeley Data public API base URL.}
}
\value{
A data frame with columns \code{name}, \code{size}, \code{sha256}, \code{content_id} and
\code{version}, one row per file.
}
\description{
Returns the public file manifest of a Mendeley Data dataset -- file names,
sizes, SHA-256 hashes and content ids -- via the Mendeley Data public REST
API. This metadata is available \strong{without authentication}; downloading the
file \emph{bytes}, however, requires an OAuth token (see \code{\link[=downloadMendeley]{downloadMendeley()}}).
}
\details{
The \code{dataset_id} is the Mendeley Data dataset identifier -- the alphanumeric
component of a Mendeley DOI \verb{10.17632/<id>.<version>} or a
\verb{data.mendeley.com/datasets/<id>/<version>} URL (both are tolerated).
}
\examples{
\dontrun{
files <- mendeleyDatasetFiles("mzpz22x58x")   # latest version
files[, c("name", "size", "sha256")]
}
}
\seealso{
\code{\link[=downloadMendeley]{downloadMendeley()}}, \code{\link[=downloadFromDOI]{downloadFromDOI()}}
}
