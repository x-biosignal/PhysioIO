% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-schema.R
\name{registerExperiment}
\alias{registerExperiment}
\title{Register a PhysioExperiment in the database}
\usage{
registerExperiment(
  con,
  x,
  experiment_id = NULL,
  subject_id = NULL,
  session_id = NULL,
  task = NULL,
  store_signals = FALSE,
  chunk_size = 10000L,
  parquet_dir = NULL
)
}
\arguments{
\item{con}{A DuckDB connection.}

\item{x}{A PhysioExperiment object.}

\item{experiment_id}{Unique identifier for the experiment.}

\item{subject_id}{Subject identifier.}

\item{session_id}{Session identifier.}

\item{task}{Task name.}

\item{store_signals}{If TRUE, stores signal data in chunks.}

\item{chunk_size}{Number of samples per chunk for signal storage.}

\item{parquet_dir}{Optional directory for a Parquet backend. When supplied,
the experiment is written with \code{\link[=writeParquet]{writeParquet()}} and its default assay is
registered as a DuckDB view named \code{<experiment_id>_signals} via
\code{\link[=registerParquetAssay]{registerParquetAssay()}}, enabling out-of-core queries over the Parquet
file without loading signals into R.}
}
\value{
The experiment_id.
}
\description{
Stores experiment metadata and optionally signal data in the database.
}
\examples{
# DuckDB is an optional dependency; guard the example so it only runs
# when the package is installed.
if (requireNamespace("duckdb", quietly = TRUE)) {
  tf <- tempfile(fileext = ".duckdb")
  con <- connectDatabase(tf)
  initPhysioSchema(con)
  # Build a small deterministic PhysioExperiment
  pe <- PhysioExperiment(
    assays = list(raw = matrix(0, nrow = 100, ncol = 3)),
    colData = S4Vectors::DataFrame(label = c("Fp1", "Fp2", "Cz")),
    samplingRate = 256
  )
  exp_id <- registerExperiment(con, pe,
    experiment_id = "demo1", subject_id = "sub01", task = "rest")
  disconnectDatabase(con)
  unlink(tf)
}
}
\references{
Raasveldt M, Muehleisen H (2019). "DuckDB: an embeddable analytical
database." Proceedings of the 2019 International Conference on
Management of Data (SIGMOD). doi:10.1145/3299869.3320212
}
\seealso{
\code{\link{loadExperiment}}, \code{\link{queryExperiments}},
\code{\link{deleteExperiment}}, \code{\link{initPhysioSchema}}
}
