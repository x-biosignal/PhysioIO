% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-prov.R
\name{writeProv}
\alias{writeProv}
\title{Serialize a provenance trail to a W3C PROV interchange file}
\usage{
writeProv(x, path, format = c("prov-json", "json-ld", "prov-n"))
}
\arguments{
\item{x}{A \code{PhysioExperiment} or a provenance \code{data.frame}.}

\item{path}{Output file path.}

\item{format}{One of \code{"prov-json"}, \code{"json-ld"}, \code{"prov-n"}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the provenance / audit trail of a \code{PhysioExperiment} (or a
provenance \code{data.frame} from \code{\link[PhysioCore]{provenance}}) to a
W3C PROV document. Three interchange syntaxes are supported: PROV-JSON
(default), PROV in JSON-LD, and PROV-N. The document round-trips exactly
through \code{\link{readProv}}.
}
\examples{
pe <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(20), 10, 2)), samplingRate = 100)
pe <- logStep(pe, "filterSignals", params = list(low = 1, high = 40))
tf <- tempfile(fileext = ".json")
writeProv(pe, tf)
readProv(tf)$activity
}
\references{
W3C PROV-JSON (Huynh et al. 2013); PROV-O; PROV-N.
}
\seealso{
\code{\link{readProv}}, \code{\link[PhysioCore]{provenance}}
}
