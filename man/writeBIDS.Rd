% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-bids.R
\name{writeBIDS}
\alias{writeBIDS}
\title{Write PhysioExperiment to BIDS format}
\usage{
writeBIDS(
  x,
  bids_root,
  subject,
  session = NULL,
  task,
  run = NULL,
  modality = c("eeg", "ieeg"),
  overwrite = FALSE
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{bids_root}{Path to the BIDS dataset root directory.}

\item{subject}{Subject identifier (without 'sub-' prefix).}

\item{session}{Session identifier (without 'ses-' prefix). Optional.}

\item{task}{Task name.}

\item{run}{Run number. Optional.}

\item{modality}{Data modality: "eeg" or "ieeg".}

\item{overwrite}{If TRUE, overwrites existing files.}
}
\value{
Invisible path to the created files.
}
\description{
Writes data in BIDS-compliant directory structure.
}
\examples{
# Create a PhysioExperiment
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000), nrow = 100, ncol = 10)),
  colData = S4Vectors::DataFrame(label = paste0("Ch", 1:10)),
  samplingRate = 100
)

# Export to a BIDS dataset in a temporary directory
bids_root <- file.path(tempdir(), "bids-write-demo")
writeBIDS(pe, bids_root, subject = "01", task = "rest")

unlink(bids_root, recursive = TRUE)
}
\references{
Gorgolewski KJ, et al. (2016). "The brain imaging data structure,
a format for organizing and describing outputs of neuroimaging
experiments." Scientific Data, 3, 160044.
doi:10.1038/sdata.2016.44
}
\seealso{
\code{\link{readBIDS}}, \code{\link{writeEDF}},
\code{\link{listBIDSSubjects}}, \code{\link{validateBIDS}}
}
