% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-clinical.R
\name{readClinicalMetadataCSV}
\alias{readClinicalMetadataCSV}
\title{Clinical metadata CSV I/O and validation}
\usage{
readClinicalMetadataCSV(
  path,
  col_map = NULL,
  required_cols = c("subject_id", "visit_id", "scale_name", "scale_score"),
  date_cols = c("assessment_date", "visit_date"),
  validate = TRUE,
  sep = ",",
  header = TRUE,
  ...
)
}
\arguments{
\item{path}{Path to the CSV/TSV file.}

\item{col_map}{Optional named character vector for renaming columns.
Names are source columns and values are target column names.}

\item{required_cols}{Required columns for validation.}

\item{date_cols}{Columns to parse as Date (\code{YYYY-MM-DD}) when present.}

\item{validate}{Logical; run \code{validateClinicalMetadata()} when TRUE.}

\item{sep}{Field separator, default \code{","}.}

\item{header}{Logical, default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{utils::read.csv()}.}
}
\value{
Data frame containing standardized clinical metadata.
}
\description{
Utilities for loading and validating clinical assessment metadata so
physiological sessions can be linked with EDC/EHR variables using
\code{subject_id} and \code{visit_id}.
Read clinical metadata from CSV
}
\examples{
tmp <- tempfile(fileext = ".csv")
write.csv(data.frame(
  sid = "S01",
  vid = "V01",
  scale_name = "FIM",
  scale_score = 90,
  assessment_date = "2026-01-10"
), tmp, row.names = FALSE)

df <- readClinicalMetadataCSV(
  tmp,
  col_map = c(sid = "subject_id", vid = "visit_id")
)
unlink(tmp)
}
\references{
Goldberger AL, et al. (2000). "PhysioBank, PhysioToolkit, and
PhysioNet: components of a new research resource for complex
physiologic signals." Circulation, 101(23), e215-e220.
doi:10.1161/01.CIR.101.23.e215
}
\seealso{
\code{\link{validateClinicalMetadata}},
\code{\link{mapClinicalCodes}}, \code{\link{readCSV}}
}
