% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge-mne.R
\name{fromMNE}
\alias{fromMNE}
\title{Convert an MNE-Python Raw object to a PhysioExperiment}
\usage{
fromMNE(raw)
}
\arguments{
\item{raw}{An MNE \code{Raw} object (e.g. from \code{\link[=toMNE]{toMNE()}} or
\code{mne.io.read_raw_*}).}
}
\value{
A \code{PhysioExperiment} with the signal in the \code{"raw"} assay.
}
\description{
Inverse of \code{\link[=toMNE]{toMNE()}}: reads the signal matrix, channel names/types, sampling
rate, electrode montage and annotations from an MNE \code{Raw} object into a
\code{PhysioExperiment}. The import is recorded in the object's provenance.
}
\examples{
if (hasMNE()) {
  pe <- PhysioExperiment(
    assays = S4Vectors::SimpleList(raw = matrix(rnorm(300), 100, 3)),
    colData = S4Vectors::DataFrame(label = c("Fp1", "Fp2", "Cz"),
                                   type = rep("eeg", 3)),
    samplingRate = 100)
  pe2 <- fromMNE(toMNE(pe))
}
}
\seealso{
\code{\link[=toMNE]{toMNE()}}, \code{\link[=hasMNE]{hasMNE()}}
}
