% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-csv.R
\name{readCSV}
\alias{readCSV}
\title{CSV/TSV I/O for PhysioExperiment}
\usage{
readCSV(
  path,
  format = c("wide", "long"),
  time_col = NULL,
  channel_cols = NULL,
  sampling_rate = NULL,
  sep = ",",
  header = TRUE,
  ...
)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{format}{Data format: "wide" (time x channels) or "long" (stacked).}

\item{time_col}{Name of the time column. If NULL, assumes first column or
generates time from sampling rate.}

\item{channel_cols}{For wide format, column names/indices to use as channels.
If NULL, uses all non-time columns.}

\item{sampling_rate}{Sampling rate in Hz. Required if time column is not present.}

\item{sep}{Column separator. Default is ",".}

\item{header}{Logical. If TRUE, first row contains column names.}

\item{...}{Additional arguments passed to read.csv/read.table.}
}
\value{
A PhysioExperiment object.
}
\description{
Functions for reading and writing signal data in CSV/TSV format.
Supports both wide format (time x channels) and long format.
Read PhysioExperiment from CSV file
}
\details{
Reads physiological signal data from a CSV file.
}
\examples{
# Write a wide-format CSV, then read it back
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(400), nrow = 100, ncol = 4)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz", "Oz")),
  samplingRate = 100
)
tmp <- tempfile(fileext = ".csv")
writeCSV(pe, tmp)

# Read using the written time column
pe_in <- readCSV(tmp, time_col = "time")

# Read without a time column (generate time from sampling rate)
pe_sr <- readCSV(tmp, channel_cols = c("Fz", "Cz", "Pz", "Oz"),
                 sampling_rate = 100)

unlink(tmp)
}
\references{
Wickham H (2014). "Tidy Data." Journal of Statistical Software,
59(10), 1-23. doi:10.18637/jss.v059.i10
}
\seealso{
\code{\link{writeCSV}}, \code{\link{readEventsCSV}},
\code{\link{readEDF}}, \code{\link{readMAT}}
}
