% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-fif.R
\name{readFIF}
\alias{readFIF}
\title{Read an Elekta/Neuromag/MNE FIF (FIFF) file}
\usage{
readFIF(path, backend = c("auto", "native", "mne"))
}
\arguments{
\item{path}{Path to a \code{.fif} file.}

\item{backend}{One of \code{"auto"} (native, then mne on failure),
\code{"native"} (native only), or \code{"mne"} (force the reticulate/mne
path).}
}
\value{
A \code{PhysioExperiment} with calibrated signal values in the
\code{"raw"} assay, channel labels/types/units in \code{colData}, and the
sampling rate set.
}
\description{
Reads a raw FIFF file into a \code{PhysioExperiment}. The native parser reads
the FIFF tag stream directly (channel info, sampling rate and data buffers).
For files the native path cannot handle, and when the Python \pkg{mne} module
is available through \pkg{reticulate}, it can fall back to
\code{mne.io.read_raw_fif()}.
}
\references{
FIFF file format (Elekta Neuromag); MNE-Python read_raw_fif.
}
\seealso{
\code{\link{readEDF}}, \code{\link{readBDF}}
}
