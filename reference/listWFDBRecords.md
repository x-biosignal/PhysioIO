# List the records in a WFDB RECORDS file

List the records in a WFDB RECORDS file

## Usage

``` r
listWFDBRecords(path)
```

## Arguments

- path:

  Path (or URL) to a PhysioNet `RECORDS` file: one record name per line.

## Value

Character vector of record names.
